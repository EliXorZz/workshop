apiVersion: v1
kind: Service
metadata:
  name: bistrot-tatina-api
  namespace: bistrot-tatina
spec:
  type: ClusterIP
  selector:
    app: bistrot-tatina-api
  ports:
    - name: http
      port: 3001
      targetPort: http
      protocol: TCP
